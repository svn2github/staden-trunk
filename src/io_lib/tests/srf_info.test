#!/bin/sh
slx_out=$top_srcdir/tests/data/slx_out

(cd $outdir; ../$top_builddir/progs/srf_info proc.srf > proc.info)
cmp $outdir/proc.info $slx_out/proc.info || exit 1

(cd $outdir; ../$top_builddir/progs/srf_info raw.srf > raw.info)
cmp $outdir/raw.info $slx_out/raw.info || exit 1

(cd $outdir; ../$top_builddir/progs/srf_info both.srf > both.info)
cmp $outdir/both.info $slx_out/both.info || exit 1


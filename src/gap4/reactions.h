#include "io_utils.h"

int find_long_gels(GapIO *io,
		   int num_contigs,
		   contig_list_t *contigs,
		   int avg_len);

int find_taq_terms(GapIO *io,
		   int num_contigs,
		   contig_list_t *contigs,
		   int avg_len);


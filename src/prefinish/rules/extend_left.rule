set name "Extend contig leftwards"

set expression "<contig_left_end>"

set pweight 0.5
set mweight 10.0

set mandatory 1
set avoid_primers 1

set solution_defaults {
    -experiments {vpwalk}
    -strand 2
}
